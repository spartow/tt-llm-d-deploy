# Router Live EPP Payload Debug Proof

## Goal

Expose the exact live EPP request payload generated by the HetroServe router before it calls the scorer.

This proves backend endpoint metrics are coming from live `/control` endpoints and include:

```json
"metrics_source": "live_control"
